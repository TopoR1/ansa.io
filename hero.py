class hero(object):
	def __init__(self, startX, startY):
		self.currX = startX
		self.currY = startY 

	def moveHero(dx, dy):
		currX += dx
		currY += dy
		if not isLegal(): pass

	def isLegal(): pass
